# 라이트 모드 딥 청록 포인트 적용

- 날짜: 2026-06-20
- 리뷰 ID: `cydytopqfvpvobnhlfmdslzncnndbtxk`

## 요청

라이트 모드에서 로고 핵심 컬러 `#24D6B5`를 그대로 쓰면 보기 힘들어, 권장한 라이트 전용 보조색을 적용한다.

## 변경

- 다크 모드는 `#24D6B5`를 유지했다.
- 라이트 모드의 `--primary`, `--primary-fill`, 강조 텍스트/버튼/선택 상태를 `#0E7490`으로 변경했다.
- 라이트 모드에서 `#0E7490` 배경 위 텍스트는 흰색으로 보정했다.
- 오류 안내 화면과 성공 토스트도 라이트 배경에서 잘 보이는 딥 청록 톤으로 조정했다.

## 확인

- 라이트/다크 모드 변수에서 서로 다른 포인트 컬러가 분리되었는지 확인했다.
